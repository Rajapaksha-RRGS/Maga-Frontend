/**
 * SupervisorPlaceholderPage.tsx
 *
 * Temporary placeholder for /supervisor until Master Prompt 3 builds
 * the full step-flow (dashboard → check-in → activity → checkout).
 */
export default function SupervisorPlaceholderPage() {
  return (
    <div className="px-4 py-8">
      <h1 className="text-base font-medium text-slate-800">Supervisor dashboard</h1>
      <p className="text-sm text-slate-500 mt-1">
        Daily time-entry flow — coming in Master Prompt 3.
      </p>
    </div>
  );
}
